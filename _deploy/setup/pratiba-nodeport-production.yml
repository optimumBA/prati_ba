apiVersion: v1
kind: Service
metadata:
  name: pratiba-nodeport-production
  namespace: production
  labels:
    app: pratiba
    env: production
spec:
  selector:
    app: pratiba
    env: production
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 4000
