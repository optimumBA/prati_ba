apiVersion: v1
kind: Service
metadata:
  name: pratiba-nodeport-staging
  namespace: staging
spec:
  selector:
    app: pratiba
    env: staging
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 4000
