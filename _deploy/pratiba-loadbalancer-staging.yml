apiVersion: v1
kind: Service
metadata:
  labels:
    app: pratiba
    env: staging
  name: pratiba-loadbalancer
  namespace: staging
spec:
  ports:
  - port: 80
    targetPort: 4000
  selector:
    app: pratiba
    env: staging
  type: LoadBalancer
